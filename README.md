# 🎶 RandomMusic Merger (Windows Forms App - C# / NAudio)

แอปพลิเคชัน Windows Forms ที่สร้างด้วย C# สำหรับรวม (Merge) ไฟล์เสียง MP3, WAV, และ M4A เข้าด้วยกันแบบสุ่ม (Random) โดยผู้ใช้สามารถกำหนดจำนวนไฟล์ผลลัพธ์และจำนวนคลิปเสียงที่จะนำมารวมในแต่ละไฟล์ได้

โปรแกรมนี้ใช้ไลบรารี **NAudio** และ **NAudio.Lame** เพื่อจัดการการประมวลผลและการเข้ารหัสไฟล์ MP3 โดยไม่ต้องพึ่งพาโปรแกรมภายนอกอย่าง `ffmpeg.exe` ทำให้ง่ายต่อการใช้งานและแจกจ่าย

---

## ✨ คุณสมบัติหลัก

* **Random Selection:** สุ่มเลือกไฟล์เสียง MP3, WAV, และ M4A จากโฟลเดอร์ต้นทาง
* **Batch Processing:** สร้างไฟล์ Output ได้หลายไฟล์ตามจำนวนที่กำหนดในการรันครั้งเดียว
* **Custom Output:** กำหนดจำนวนคลิปเสียงที่จะนำมารวมกันเป็น 1 ไฟล์ Output
* **Standalone:** ใช้ NAudio และ NAudio.Lame ในการรวมและ Encode MP3 โดยสมบูรณ์ (ไม่ใช้ FFmpeg)
* **Unique Filenames:** ใช้ Timestamp (วันเดือนปี_เวลา_ลำดับ) เพื่อให้ชื่อไฟล์ Output ไม่ซ้ำกัน

---

## 🛠️ การติดตั้งและรัน

### 1. ข้อกำหนดเบื้องต้น

* **Visual Studio:** (แนะนำ) สำหรับการคอมไพล์โปรเจกต์
* **.NET Framework:** โปรเจกต์นี้กำหนดเป้าหมายเป็น **.NET Framework 4.6.1** หรือสูงกว่า

### 2. การติดตั้ง Dependencies (NuGet)

โปรเจกต์นี้ต้องใช้ 2 NuGet Package หลัก:

เปิด **Package Manager Console** ใน Visual Studio แล้วรันคำสั่ง:

```bash
Install-Package NAudio
Install-Package NAudio.Lame
